name: CI Probe
on:
  push:
    branches: [newmain]
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "push probe ok on ${{ github.ref }} sha=${{ github.sha }}"